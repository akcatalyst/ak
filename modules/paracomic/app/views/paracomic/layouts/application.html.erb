<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Paracomic - Comedy Shows" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-br from-purple-50 to-pink-50 min-h-screen">
    <!-- Paracomic Header -->
    <header class="bg-white shadow-sm border-b border-purple-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center">
            <%= link_to root_path, class: "text-xl font-bold text-purple-900 hover:text-purple-600 transition duration-200" do %>
              ← Back to Akcatalyst
            <% end %>
          </div>
          <div class="flex items-center space-x-4">
            <h1 class="text-2xl font-bold text-purple-700">🎭 Paracomic</h1>
          </div>
        </div>
      </div>
    </header>

    <!-- Main Content -->
    <main class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-purple-200 mt-auto">
      <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
        <div class="text-center">
          <p class="text-sm text-gray-600">
            Comedy shows powered by <span class="font-semibold text-purple-600">Akcatalyst</span> - 
            <%= link_to "Return to main site", root_path, class: "text-purple-600 hover:text-purple-800" %>
          </p>
        </div>
      </div>
    </footer>
  </body>
</html> 